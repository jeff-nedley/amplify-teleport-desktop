# Core dependencies
PySide6>=6.6.0                      # cross-platform UI (Windows + macOS)
Pillow>=10.0.0                      # icon conversion / assets

# Networking, async, and WebRTC (from aiortc in teleport.py)
requests>=2.28.0
aiortc>=1.3.0
aioice>=0.9.0

# Packaging
pyinstaller>=6.0.0

# Windows-only
pywin32>=306; sys_platform == "win32"

# macOS-only — native menu bar status item (NSStatusItem) + no Dock icon
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
